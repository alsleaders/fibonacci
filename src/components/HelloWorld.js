import React, { useState } from 'react'

export default function HelloWorld() {
  // const newValue = seq[i - 1] + seq[i - 2]
  // const [seq, setSeq] = useState([1, 1])

  // const addFib = () => {
  //   // add seq + newValue
  // }
  return (
    <>
      <h1>Hello, World!</h1>
      {/* <button onClick={addFib}>Next number</button>
      {seq.map(item => (
        <h3>{seq.value}</h3>
      ))} */}
    </>
  )
}
